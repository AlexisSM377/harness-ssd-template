# Implementation Plans

Generated by `/improve plan` (001) and `/improve` (002-006) on 2026-07-28.
002-006 come from auditing a separate, unrelated project (`odc`) that was
built using this template — used only as real-world evidence of where the
template itself has gaps. No code in that project was read for any purpose
beyond identifying these findings, and none of it was modified.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add a flat, prefix-named `skills/` convention to the template | P1 | S | — | DONE (merged to `main`, commit `9056fc3`) |
| 002  | Add a checkpoint that catches dead code left by a superseded feature | P1 | S | — | DONE (merged to `main`, commit `7d3faa7`) |
| 003  | Catch STATUS.md drift automatically, require it at session close | P1 | S | — | DONE (merged to `main`, commit `e0e124f`) |
| 004  | Require new env vars documented in the same commit that introduces them | P2 | S | — | DONE (merged to `main`, commit `fe33ead`) |
| 005  | Add a TYPECHECK_CMD slot to init.config.sh / init.sh | P2 | S | — | DONE (merged to `main`, merge commit `4e38b67`) |
| 006  | Tell skill installers to gitignore local state/cache dirs | P3 | S | — | DONE (merged to `main`, merge commit `63ef40a`) |

## Dependency notes

- None of 002-006 depend on each other or on 001 — each touches a distinct
  set of files (see each plan's Scope) and can execute in any order.
- 002 and 004 both touch `template/AGENTS.md`, but different sections (§7
  close-out vs §4 hard rules) — safe to run sequentially, just re-read the
  file between them since line numbers will have shifted after the first.

## Execution notes

- 001 executed by a dispatched executor in an isolated worktree, reviewed,
  approved, and merged to `main` on 2026-07-28 (commit `9056fc3`). Worktree
  and its branch were cleaned up after merge.
- Plan authoring bug found during review, not an implementation defect: the
  plan's own verification command for "no nested skill folders"
  (`find template/.claude/skills -mindepth 2 -name SKILL.md`) has an
  off-by-one — `mindepth 2` already matches the flat, correct structure
  (`<skill>/SKILL.md`) that Step 2 creates. The check that actually detects
  *category* nesting is `-mindepth 3`. The executor caught this, ran both,
  and confirmed the real structure is flat (no true nesting) before
  proceeding. Fix this command if plan 001 is ever reused as a reference.
- 002 executed by a dispatched executor in an isolated worktree, reviewed,
  approved, and merged to `main` on 2026-07-28 (commit `7d3faa7`). Worktree
  and its branch were cleaned up after merge.
- 003 executed by a dispatched executor in an isolated worktree, reviewed,
  approved, and merged to `main` on 2026-07-28 (merge commit `e0e124f`).
  The executor's worktree branch (`worktree-agent-af357e5f35229f6b5`) had
  based itself on `9056fc3` rather than the then-current `main` tip
  (`7d3faa7`, already including plan 002) — an infra quirk in how the
  worktree was provisioned, not a plan or executor error. Since 002 and 003
  touch fully disjoint files, this required a real merge commit instead of
  a fast-forward, but produced no content conflicts. Worth watching on
  future dispatches: re-run each plan's drift check against current `main`
  HEAD, not just the plan's own "Planned at" SHA, before trusting a
  worktree's base is current.
- Plan authoring bug found during 003's review, not an implementation
  defect: the plan's own verify command (`grep -n "Features completadas"
  template/init.sh` → expected 1 match) undercounts, because the plan's own
  prescribed code block contains the string "Features completadas" twice
  (once in the regex, once in the `NO_MATCH` warn message) — real match
  count is 2. The executor flagged this correctly rather than silently
  "fixing" the count. Same class of issue as 001's `-mindepth` bug: fix the
  verify command's expected count if plan 003 is ever reused as a
  reference.

## Findings considered and rejected

- Replicating the screenshot's nested `skills/<category>/<skill>/` folder
  structure literally: rejected. Confirmed against
  anthropics/claude-code#28266 and #40640 that Claude Code only scans
  `.claude/skills/` one level deep — two-level nesting silently fails to
  auto-load. This is the actual root cause of the auto-detect problem the
  user reported, not something the reorg should reproduce.
- Adding `hooks/`, `MEMORY.md`, `run.sh`, `install.sh`, `.mcp.json` (the rest
  of the screenshot's "loopkit" vault layout): rejected — user confirmed
  scope is skills/ only. That vault is a different harness style (autonomous
  loop runner) than this repo's spec-gated, human-approval model; importing
  its scaffolding wholesale isn't a fit.
